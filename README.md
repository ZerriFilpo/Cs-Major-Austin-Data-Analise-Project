ENGLISH

# CS Major Austin Data Analysis Project

Stats explorer for the **BLAST.tv Austin Major 2025** in CS2 using **Python + SQLite**.  
The idea is to build a small **“offline HLTV”** focused on this event: store all data in a local database and then run custom queries and analyses (top players, CT/T bias per map, pistol conversions, etc.).

---

## 🎯 Project goals

- Design and create a **SQLite** database for:
  - tournaments
  - teams
  - players
  - coaches
  - matches (BO1 / BO3 / BO5)
  - maps played in each match
  - per-player stats per map
  - side (CT/T) performance and pistols
  - https://dbdiagram.io/d/CS-DATA-BASE-693c28b0e877c630749eec0b

- Build, in Python:
  - a database access layer (`db.py`)
  - a stats/logic layer (`stats_service.py`)
  - a simple CLI menu (`main.py`) to explore the data

Later, the plan is to **import data from HLTV** (for BLAST Austin 2025) into this schema, either manually or via a scraping/integration script.

---

## 🧱 Tech stack

- **Language:** Python 3  
- **Database:** SQLite (`cs_major_stats.db`)  
- **Version control:** Git + GitHub  

---

## 📁 Project structure

cs_major_stats/
  README.md
  main.py            # CLI menu / user interaction
  db.py              # SQLite connection + table creation + insert helpers
  models.py          # (optional) domain models / dataclasses (Player, Team, etc.)
  stats_service.py   # queries and stats built on top of the DB
  cs_major_stats.db  # SQLite database file (generated by the code)

PORTUGUESE

# CS Major Austin Data Analise Project

Explorador de estatísticas do **BLAST.tv Austin Major 2025** em CS2, usando **Python + SQLite**.  
A ideia é criar um mini-“HLTV offline” focado nesse torneio: armazenar todos os dados em um banco local e depois fazer consultas e análises (top jogadores, mapas mais CT/T, pistols, etc.).

---

## 🎯 Objetivo do projeto

- Modelar e criar um banco de dados em **SQLite** com:
  - torneios
  - times
  - jogadores
  - coaches
  - partidas (BO1/BO3/BO5)
  - mapas jogados em cada partida
  - estatísticas por jogador em cada mapa
  - informações de lado (CT/T) e pistols
  - https://dbdiagram.io/d/CS-DATA-BASE-693c28b0e877c630749eec0b

- Construir, em Python:
  - uma camada de acesso ao banco (`db.py`)
  - uma camada de lógica de estatísticas (`stats_service.py`)
  - um menu simples de console (`main.py`) para explorar os dados

No futuro, o objetivo é preencher o banco automaticamente via scraping/integração com dados da HLTV para o BLAST Austin 2025.

---

## 🧱 Tecnologias

- **Linguagem:** Python 3
- **Banco de dados:** SQLite (`cs_major_stats.db`)
- **Controle de versão:** Git + GitHub

---

## 📁 Estrutura do projeto

cs_major_stats/
  README.md
  main.py            # menu / interface de linha de comando
  db.py              # conexão com SQLite + criação de tabelas + funções de inserção
  models.py          # (opcional) classes / dataclasses de domínio (Player, Team, etc.)
  stats_service.py   # consultas e estatísticas em cima do banco
  cs_major_stats.db  # arquivo do banco de dados (gerado pelo código)
